#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alx[100];

	strcpy(alx, "\"Programming is like building a multilingual puzzle");

	puts(alx);
	return (0);
}
